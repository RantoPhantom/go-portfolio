version: "2"

sql:
    - engine: "sqlite"
      queries: "../database/queries/user_queries.sql"
      schema: "../database/schemas/user_db.sql"
      gen:
          go:
              package: "database"
              out: "../database"

    - engine: "sqlite"
      queries: "../database/queries/session_queries.sql"
      schema: "../database/schemas/sessions_db.sql"
      gen:
          go:
              package: "database"
              out: "../database"
